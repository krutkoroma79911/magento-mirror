<?php

/**
 * @var Tsg_Trial_Block_Advertising $this
 */
$product = $this->getProduct();
$sku = $product ? $product->getSku() : null;
if ($sku === 'advertising' || $sku == null) {
    echo $this->getAdvertising();
}
?>